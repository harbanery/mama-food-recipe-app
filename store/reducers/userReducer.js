const initialState = {
  user: {},
  myRecipes: [],
  savedRecipes: [],
  likedRecipes: [],
};

const userReduces = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
